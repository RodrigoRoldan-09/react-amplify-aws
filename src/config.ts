// Archivo de configuración para el API Gateway URL
// Crear como src/config.ts

// Reemplaza con la URL real de tu API Gateway generada por CDK
const config = {
    // URL completa con fase de despliegue (ejemplo: https://abc123def.execute-api.us-east-1.amazonaws.com/prod)
    apiGatewayUrl: 'https://your-api-gateway-id.execute-api.us-east-1.amazonaws.com/prod'
  };
  
  export default config;